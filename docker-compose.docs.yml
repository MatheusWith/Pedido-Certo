services:
  docs:
    image: pedido_certo_local_docs
    container_name: pedido_certo_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./pedido_certo:/app/pedido_certo:z
    ports:
      - '9000:9000'
    command: /start-docs
